# CM-007 — Keyboard scrolling fix

- The tasting wizard keeps its stable full-screen height while the keyboard is open.
- The keyboard area is represented as scroll padding instead of shrinking the app.
- Active fields are scrolled into the visible visual viewport.
- The wizard is fixed to the viewport so page content cannot slide behind an empty black band.
